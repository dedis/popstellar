{
	"$schema": "http://json-schema.org/draft/2019-09/schema#",
	"$id": "http://pop2020.dedis.ch/answer/positiveAnswerGeneral.json",
	"title": "All requests (except catchup (list of message) and propagate message (nothing)) expect an ACK (0) as answer",
	"description": "[Int] success result message",

	"type": "integer",
	"minimum": 0, "maximum": 0, 
	"$comment": "Note: should always be 0" 
}
