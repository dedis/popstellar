<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

  <data>

    <import type="android.view.View" />

    <import type="com.github.dedis.popstellar.model.objects.event.EventState" />

    <variable
      name="event"
      type="com.github.dedis.popstellar.model.objects.event.Event" />
  </data>

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
      android:id="@+id/public_key"
      android:layout_width="@dimen/wallet_attendee_width"
      android:layout_height="wrap_content"
      android:layout_marginLeft="@dimen/margin_text_small"
      android:text=""
      android:textSize="@dimen/size_body" />

    <ImageView
      android:id="@+id/pk_qr_code"
      android:layout_width="@dimen/wallet_qr_code_size"
      android:layout_height="@dimen/wallet_qr_code_size" />
  </LinearLayout>

</layout>
