{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/dedis/student_21_pop/master/protocol/query/method/message/data/dataDeletePostBroadcast.json",
  "description": "Match a delete post broadcast query",
  "type": "object",
  "properties": {
    "object": {
      "const": "post"
    },
    "action": {
      "const": "delete"
    },
    "post_id": {
      "description": "message_id of the post message above",
      "type": "string",
      "contentEncoding": "base64"
    },
    "channel": {
      "description": "The channel where the post is located (starting from social/ inclusive)",
      "type": "string",
      "pattern": "^social(/[^/]+)*$",
      "$comment": "Note: the regex matches a \"social\" or a \"social/<channel>\""
    },
    "timestamp": {
      "description": "UNIX Timestamp in UTC given by the message above (deletion request)",
      "type": "integer",
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": ["object", "action", "post_id", "channel", "timestamp"]
}