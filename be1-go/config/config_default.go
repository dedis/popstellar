// +build !testing

// this file has currently the same setting as the test config, but can become more useful in the future if different builds would be used
package config

const (
	MODE    = "o"
	ADDRESS = "127.0.0.1"
	PORT    = 8000
	PKEY    = "oui"
	FILE    = "default"
)
