{
    "$comment": "If an error occurred, the “result” field must be absent and the error object must adhere to the following (extensible) format",
    "$schema": "http://json-schema.org/draft/2019-09/schema#",
    "$id": "http://pop2020.dedis.ch/errors/error.json",
    "type": "object",
    "properties" : {
        "jsonrpc": {
            "const": "2.0"
        },
        "error": {
            "description": "For responses, absent on success, present on error",
            "type": "object",
            "properties" : {
                "code" : {
                    "description": "error code",
                    "type": "integer",
                    "enum": [0,-1,-2,-3,-4,-5],
                    "$comment":  "0 operation was successful (should never be used)",
                    "$comment":  "-1 invalid action",
                    "$comment":  "-2 invalid resource (e.g. channel does not exist, channel was not subscribed to, etc.)",
                    "$comment":  "-3 resource already exists (e.g. lao already exists, channel already exists, etc.)",
                    "$comment":  "-4 request data is invalid (e.g. message is invalid), use the data field object to provide extra information",
                    "$comment":  "-5 access denied (e.g. subscribing to a “restricted” channel)"
        
                },
                "descri": {
                    "description": "error description",
                    "type": "string"
                },
                "required": ["code", "descri"]
            }
        },
        "id": {
            "description": "Unique identifier for the request",
            "type": "integer"
        }

    },
    "required": ["jsonrpc", "error", "id"]
}