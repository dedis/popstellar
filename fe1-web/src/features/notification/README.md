# Notification

Provides general support for notifications inside a LAO. Allows other features
to create different types of notifications, stores references to them in a
global state that allows them to displayed in a unified notification screen.

## Dependencies

- Basic LAO functionality provided by the `lao` feature

See `interface/Configuration.ts` for more details.
