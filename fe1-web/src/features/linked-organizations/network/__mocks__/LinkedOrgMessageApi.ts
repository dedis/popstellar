/**
 * Mocks functions to send linked organizations messages.
 */
export const requestChallenge = jest.fn(() => Promise.resolve());
export const initFederation = jest.fn(() => Promise.resolve());
export const expectFederation = jest.fn(() => Promise.resolve());
