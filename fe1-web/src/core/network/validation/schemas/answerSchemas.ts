import answer from 'protocol/answer/answer.json';
import error from 'protocol/answer/error.json';
import messages_by_channel from 'protocol/answer/result/messages_by_channel.json';

const answerSchemas = [answer, error, messages_by_channel];

export default answerSchemas;
